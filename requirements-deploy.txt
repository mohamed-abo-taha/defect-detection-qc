# CPU-only deploy (Streamlit Community Cloud: no GPU, ~1 GB RAM).
# Installs CPU torch wheels and only what the CLASSIFICATION demo needs.
# (ultralytics/onnx are heavier — keep the detection track for local/CLI use.)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
torchvision==0.21.0+cpu
timm>=1.0
numpy>=1.26
scikit-learn>=1.3
matplotlib>=3.7
pillow>=10
streamlit>=1.30
