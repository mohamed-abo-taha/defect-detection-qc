# Local / GPU development install.
# For a CPU-only cloud deploy (e.g. Streamlit Community Cloud) use requirements-deploy.txt.
# For GPU training, install a CUDA-matched torch first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.2
torchvision>=0.17
timm>=1.0
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
pillow>=10
opencv-python>=4.9
tqdm>=4.66
pyyaml>=6
onnx>=1.16
onnxruntime>=1.17
ultralytics>=8.1
streamlit>=1.30
pytest>=8
